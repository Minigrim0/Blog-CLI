stages:
  - build
  - test
  - deploy

build-job:
  stage: build
  tags:
    - rust
  script:
    - cargo build --release

unit-test-job:
  stage: test
  tags:
    - rust
  script:
    - cargo test

deploy-job:
  stage: deploy
  tags:
    - rust
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+\-release/'
  environment: production
  script:
    - cargo publish
